# Statecharts

State machines in the **XStateV5-like config format** (not a dependency), executed by an rx-toolkit runtime.

- The machine snapshot is an ordinary signal.
- Can be generated with `@fozy-labs/statechart-converter`
